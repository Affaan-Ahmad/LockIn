node_modules/
.next/
# Isolated output for verification builds, so they never touch the dev server.
.next-verify/
out/
dist/
coverage/
*.tsbuildinfo
# Every env file except the committed template. The previous patterns missed
# `.env.local.bak`, which is exactly the shape a hand-made backup takes, and
# GitHub's push protection was the only thing that stopped it being published.
.env
.env.*
!.env.example
.DS_Store
.vercel
